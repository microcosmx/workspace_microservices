# HTTP Server
server:
  port: 18886  # HTTP (Tomcat) port

spring:
  data:
    mongodb:
      host: ts-rebook-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017


  zipkin:
    baseUrl: http://zipkin:9411/
  sleuth:
    sampler:
      percentage: 1.0