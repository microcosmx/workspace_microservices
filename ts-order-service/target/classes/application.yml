# HTTP Server
server:
  port: 12031  # HTTP (Tomcat) port

spring:
  data:
    mongodb:
      host: order-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
#      database: piggymetrics
      port: 27017