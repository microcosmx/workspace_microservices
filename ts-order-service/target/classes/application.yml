# HTTP Server
server:
  port: 12031  # HTTP (Tomcat) port

spring:
  data:
    mongodb:
      host: ts-order-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
#      database: piggymetrics
      port: 27017