spring:
  application:
    name: account-register-service
    data:
      mongodb:
        uri: mongodb://localhost:27017/jcTestMongo

# HTTP Server
server:
  port: 12344  # HTTP (Tomcat) port

# Discovery Server Access
eureka:
  aclient:
    serviceUrl:
    defaultZone: http://localhost:8761/eureka/
