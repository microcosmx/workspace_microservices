复现的是 异步过程顺序错误 
复现过程是：退票的时候，第一个异步线程会调用退款服务，第二个异步线程调用订单服务修改状态
            第一个线程会检查订单的状态，在订单处于取消状态的时候会退款失败；
            第二个线程会修改订单状态为已取消。
            第一个县城有2秒的sleep，使得第二个线程抢先完成，导致第一个线程无法完成所有流程。
日志效果：两个execution里长一些的是正常流程，短一些的是顺序错误以后第一个线程没走完的流程。