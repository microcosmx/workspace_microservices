# HTTP Server
server:
  port: 19001  # HTTP (Tomcat) port

spring:
  data:
    mongodb:
      host: ts-payment-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017