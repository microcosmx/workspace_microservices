spring:
  data:
    mongodb:
      host: ts-insite-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017

# HTTP Server
server:
  port: 12387  # HTTP (Tomcat) port