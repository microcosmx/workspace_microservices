spring:
  data:
    mongodb:
      host: ts-execute-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017

# HTTP Server
server:
  port: 12386  # HTTP (Tomcat) port