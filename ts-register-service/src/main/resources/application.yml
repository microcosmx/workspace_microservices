spring:
  data:
    mongodb:
      host: ts-register-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: jichao
      port: 27017

# HTTP Server
server:
  port: 12344  # HTTP (Tomcat) port