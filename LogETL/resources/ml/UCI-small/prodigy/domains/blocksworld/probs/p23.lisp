(load-goal '(and (holding C) (on A B)))

(load-start-state
   '((object C)
     (on-table C)
     (object B)
     (on-table B)
     (object A)
     (on-table A)
     (clear A)
     (clear B)
     (clear C)
     (arm-empty)))

