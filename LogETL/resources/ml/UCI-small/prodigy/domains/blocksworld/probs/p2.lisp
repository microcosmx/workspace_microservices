(load-goal '(arm-empty))

(load-start-state '((object A)
             (object B)
             (holding A)
             (on B C)
             (clear B)
             (object C)
             (on-table C)))

