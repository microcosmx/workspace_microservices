前置条件：处于已付款状态的“订单其它”
操作：点击退票按钮
结果：退单成功，订单状态设置成Cancel；退单失败，订单状态设置成Cancelling

实现逻辑:
点击退票按钮后，调用ts-cancel-service，cancel服务先将订单状态设置成Cancelling，再调用ts-inside-payment-service退款，
站内支付服务退款成功后，会将订单状态设置成Cancel。但是为了提高执行速度，这两步以异步的方式执行，一般来说也是先Cancelling再
Cancel，但是某些情况下（如网络状况不好），导致先执行完退款操作，再执行了设置Cancelling操作，导致订单状态错误。在退款时，用户
获得的退款并不是原先为订单支付的全部金额，会根据退票时间来进行计算，过了乘车时间退票，退款为0；退款时间离乘车时间越近，用户
拿到的退款越少。为了提高响应，站内支付服务一开始退款的时候，退款金额是退票服务处直接拿到的，会直接执行退款。但是这样做，有一定
的问题，如果中间因为网络问题，站内支付服务拿到的退款金额可能是一个根据很早之前的时间算得的金额，于是站内支付服务需要做一次检查，
再次要求退款服务算一次退款金额，如果两次不一样的话，会删除上次的退款，重新退款，并重置订单状态为Cancel。于是这种情况下，订单状态
如果是Cancelling，会被重置为Cancel。

