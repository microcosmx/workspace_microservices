spring:
  data:
    mongodb:
      host: train-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
#      database: piggymetrics
      port: 27017

# HTTP Server
server:
  port: 14567  # HTTP (Tomcat) port