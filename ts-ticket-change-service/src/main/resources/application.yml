spring:
  data:
    mongodb:
      host: ts-ticket-change-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017

# HTTP Server
server:
  port: 14339  # HTTP (Tomcat) port