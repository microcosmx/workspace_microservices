spring:
  data:
    mongodb:
      host: preserve-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
#      database: piggymetrics
      port: 27017

# HTTP Server
server:
  port: 14568  # HTTP (Tomcat) port