spring:
  data:
    mongodb:
      host: ts-login-mongo
#      username: user
#      password: ${MONGODB_PASSWORD}
      database: jichao
      port: 27017

# HTTP Server
server:
  port: 12342  # HTTP (Tomcat) port